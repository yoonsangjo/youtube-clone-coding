extends ../base
include ../mixins/video

block content 
    div.profile__data
        if !user.avatarUrl
            span.profile__icon 😊
        else
            img(src='/' + user.avatarUrl, alt="").avatar
        h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__massage Sorry nothing found.